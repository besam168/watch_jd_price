
- 2026-04-27 晚间补记：`auction_915_925_smooth_scanner_v2` 已正式从单点 09:24:30 任务升级成 **两段式调度**：
  1. `沈万三_集合竞价狙击手V2_0915_Capture` → 09:15 启动连续采样；
  2. `沈万三_集合竞价狙击手V2_092430_Judgement` → 09:24:30 读取轨迹并输出名单；
  同时已关闭旧的 V2 单点任务 `沈万三_集合竞价狙击手V2_092430`，避免自相冲突。
- 2026-04-27 晚间手动自检结论：
  - `run_capture_0915.py --force` 已手动跑通；
  - `run_judgement_092430.py --force` 初次暴露 Windows GBK 控制台打印报错，已补 `safe_print_json()` 修复；
  - 修复后两段式链路可手动完整通过，说明明天至少从“任务链是否能跑”角度看已过关。
- 2026-04-27 晚间进一步收口：已把 V2 的股票名显示改成**优先走本地 `name_map.csv` 映射**，不再优先信任 pytdx 返回的脏名字字段；后续若映射表覆盖充分，显示层应明显比今天白天更干净。
