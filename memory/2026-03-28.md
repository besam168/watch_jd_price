# 2026年3月28日 工作记忆

---

## 今日总结（00:00 左右收口）

### 1. GitHub 上传主线正式打通
- 先完成了 `desktop-input-control v2.0` 的 GitHub 上传。
- 过程中先走 HTTPS，遇到两类真实问题：
  - 认证弹窗被取消：`fatal: User cancelled dialog.`
  - 网络连接被重置：`Recv failure: Connection was reset`
- 后续改走 **SSH** 路线，生成本机 `ed25519` key，并让用户添加到 GitHub。
- SSH 验证成功后，`desktop-input-control` 已成功 push 到：
  - `https://github.com/besam168/desktop-input-control`

### 2. 成功上传了 4 个核心插件/技能仓库
#### 已上传成功
1. `desktop-input-control`
   - 仓库：`https://github.com/besam168/desktop-input-control`
   - 关键提交：`a00ab00`
   - 说明：当前是 v2.0，包含屏幕截图与 OCR 能力

2. `gemini-bridge`
   - 仓库：`https://github.com/besam168/gemini-bridge`
   - 通过 `git subtree split --prefix=skills/gemini-bridge` 拆分后上传

3. `claude-code-bridge`
   - 仓库：`https://github.com/besam168/claude-code-bridge`
   - 通过 `git subtree split --prefix=skills/claude-code-bridge` 拆分后上传

4. `telegram-image-sender`
   - 仓库：`https://github.com/besam168/telegram-image-sender`
   - 通过 `git subtree split --prefix=skills/telegram-image-sender` 拆分后上传

### 3. GitHub 上传路线的稳定结论
- 这台机器当前上传 GitHub，**SSH 比 HTTPS 稳定得多**。
- HTTPS 路线容易卡在：
  - Git Credential Manager / 浏览器认证交互
  - `Connection was reset`
- SSH 路线在用户把公钥加进 GitHub 后，已真实验证成功。

### 4. 盘点结果
- 这几天最核心、最值得上传的主成果，已完成上传：
  - `desktop-input-control`
  - `gemini-bridge`
  - `claude-code-bridge`
  - `telegram-image-sender`
- 其余 `skills/` 下目录暂未继续无脑上传，原因是有些属于旧存量、第三方基础技能或可能带测试/环境内容，后续应先筛选再传。

### 5. 补充判断
- `telegram-image-sender` 这次是按当前目录原样上传，仓库里可能带有测试截图与 `output` 产物。
- 若后续要做“发布版整理”，应考虑：
  - 增加 `.gitignore`
  - 清理测试截图/输出文件
  - 补 README

---

## 当前状态
- GitHub 上传主线已打通。
- `besam168` 账号下已有 4 个成功上传的仓库。
- 后续如继续上传第二批技能，优先延续 **SSH + subtree split** 路线。

### 6. 2026-03-28 凌晨报告主线进展
- 用户随后提供了一批国际新闻与交易所/财经站链接，希望据此整理正式版《全球综合情报报告》。
- 已实际抓取并交叉查看的核心来源包括：`Reuters`、`AP`、`BBC`、`Al Jazeera`、`CNBC`、`Yahoo Finance`、`NYSE`、`TWSE`、`SSE`、`Investing.com`，以及 `JPX / KRX / Naver Finance` 的补充页面。
- 抓取阶段的稳定经验：国际新闻首页与部分财经首页可直接用 `web_fetch` 获取，但 `JPX / KRX / Naver` 等站点返回内容有时不完整，后续做正式报告时需要交叉校验，不能单点采信。
- 截至本次记忆 flush 前，报告仍处于“已抓到源、正在整理正文”的阶段，**尚未发邮件**。
- 用户在清晨再次确认“在”，已向用户汇报：当前仍在整理正式版报告，邮件发送尚未执行。

### 7. 天猫精灵语音桥项目（试运行）
- 已启动一个新项目方向：`tmall-genie-voice-bridge`，目标是让 OpenClaw 以后能通过“天猫精灵语音桥”接入家庭语音场景。
- 已先确认家中局域网可见设备 `Tmall-Genie`，IP 为 `192.168.2.136`；随后明确：当前家庭里没有“小爱音箱”，目标设备改为天猫精灵。
- 项目第一版没有承诺“直接接管天猫精灵麦克风”，而是按更现实的路线先做：
  - 本地 bridge server
  - 文本 -> TTS
  - 播放后端适配层
  - mock 播放链路
- 已在工作区创建：`skills/tmall-genie-voice-bridge/`
- 第一版关键提交：
  - `9c5a7f3` - `Add tmall genie voice bridge MVP skill`
  - `fac81f8` - `Clean tmall genie bridge artifacts and ignore local files`
- 已完成第二轮清理：
  - 移除 `config.json`、`tmp_audio`、`__pycache__`、`*.pyc` 的版本控制痕迹
  - 新增 `.gitignore`
  - 补强 `SKILL.md` 快速上手说明
- 当前确认可用的状态：
  - `speak.py` + `mock_tmall_genie` 链路已跑通
  - 真实天猫精灵播报/语音入口仍待第二阶段选型与集成

### 8. 天猫精灵语音桥仓库命名与新项目建仓规则进一步确认
- 用户已正式确认本项目英文名 / 仓库名使用：`tmall-genie-voice-bridge`。
- 用户已创建 GitHub 仓库：`https://github.com/besam168/tmall-genie-voice-bridge`。
- 用户再次明确：以后只要开新项目，都要先提醒他：
  1. 先起项目英文名
  2. 再创建新的独立 GitHub 仓库地址
  3. 再推进上传与发布
- 这条规则应视为默认长期工作流，不再把新项目长期混在旧总仓库里。

### 9. 天猫精灵语音桥独立仓库迁移进展
- 已开始将 `skills/tmall-genie-voice-bridge` 从总工作区仓库拆分到独立仓库。
- 已执行 `git subtree split --prefix=skills/tmall-genie-voice-bridge`，并创建导出分支：`tmall-genie-voice-bridge-export`。
- push 到独立仓库的动作已发起，但在本次 memory flush 时尚未确认最终结果；后续会继续检查并补齐。

### 10. 天猫精灵语音桥独立仓库已完成上传
- 用户确认改走 SSH 后，已将独立仓库 remote 从 HTTPS 改为 SSH：`git@github.com:besam168/tmall-genie-voice-bridge.git`
- 已成功 push 到独立仓库：`https://github.com/besam168/tmall-genie-voice-bridge`
- 当前结论：`tmall-genie-voice-bridge` 已不只是总工作区子目录，而是已有独立 GitHub 仓库。

### 11. 天猫精灵语音桥第二阶段推进现状
- 用户要求恢复“项目经理 + 两个 AI 助手”的推进方式，但本轮实际结果不稳定：
  - Claude Code 多次未形成有效工程产出
  - Gemini 多次超时、失败或明显跑偏题
- 用户随后明确授权：当 Claude Code 不跑时，可以由我直接接管实现，不必死等。
- 当前真实状态：第二阶段尚未完成，仍不能宣称“已经控制天猫精灵”或“已经打通真实播放到天猫精灵”。
- 第二阶段当前唯一明确目标：先做**本机临时真实播放端**，也就是先让电脑本机真实播放 TTS 生成的 mp3，再考虑迁移到天猫精灵播报。

### 12. 本机真实播放端的阶段性技术结论
- 已确认本机默认假设的 `127.0.0.1:8123` 没有服务，因此当前 `local_http_player` 没有真实目标可打。
- 已确认一个现成 mp3 可用于测试：`skills/tmall-genie-voice-bridge/tmp_audio/20260328-073828-d2627ec6.mp3`
- 已验证：
  - Python 版 `local_audio_player.py` 走 `WMPlayer COM` 时卡在 `pywin32` 缺失，不能作为当前最省事路线。
  - 但 **PowerShell 原生 COM 可以直接调起 Windows Media Player 播放 mp3**，测试返回 `PLAYSTATE=9`，说明 Windows 本机原生播放链路可用。
- 因此当前最省事的最小实现路线，已从“Python + pywin32”收敛为：
  - **PowerShell 原生播放脚本**
  - 再由 Python 后端包装调用

### 13. 已新增但尚未完成总验收的文件
- 为推进第二阶段，已新增：
  - `skills/tmall-genie-voice-bridge/scripts/local_audio_player.py`
  - `skills/tmall-genie-voice-bridge/scripts/play-local-audio.ps1`
  - `skills/tmall-genie-voice-bridge/scripts/backends/local_windows_speaker.py`
- 这些文件的定位是：
  - 不重造 TTS
  - 只补“本机真实播放后端”
  - 作为从 mock 链路过渡到真实播放链路的中间层
- 截至本次 memory flush：
  - 新后端**还未完成**接入 `speak.py` / 配置注册 / 最终真实播放验收
  - 因而第二阶段仍处于“已找到可行播放路径、已落部分文件、未完成总验收”的状态

### 14. 用户对推进方式的新要求
- 用户明确要求：以后推进这类项目时，要按“项目经理模式”执行：
  1. 加大速度
  2. 每做一步，立刻报告一步
  3. 不要闷头做完再统一汇报
- 用户还提醒了一个关键原则：
  - 现成 `edge-tts` / `aliyun-tts` 已经能负责“文本 -> 语音”
  - 新项目不应重复造 TTS 核心能力
  - 项目应聚焦：**播放端、桥接层、真实出声链路**

### 15. Codex CLI 已成功接入第三方 OpenAI 兼容 coding 模型
- 用户提供了 Feishu 教程，确认 `Codex CLI` 可通过编辑 `C:\Users\besam\.codex\config.toml` 与 `C:\Users\besam\.codex\auth.json` 接入第三方 OpenAI 兼容接口，而不必再改 OpenClaw 主配置。
- 本机已确认安装 `Codex CLI`：
  - 路径：`C:\Users\besam\AppData\Roaming\npm\codex.ps1`
  - 版本：`codex-cli 0.117.0`
- 为避免破坏原有登录态，已先备份：
  - `C:\Users\besam\.codex\config.toml.before-92scw-gpt-5-3-codex.bak`
  - `C:\Users\besam\.codex\auth.json.before-92scw-gpt-5-3-codex.bak`
- 随后已按教程改写 Codex 配置，使其走用户指定模型：
  - `model_provider = "codex"`
  - `model = "gpt-5.3-codex"`
  - `base_url = "http://92scw.cn/v1"`
  - `wire_api = "responses"`
  - `requires_openai_auth = true`
- `auth.json` 已改为最简 API key 方案，用于该 OpenAI 兼容接口认证。
- 已执行最小真实验收：让 `codex exec` 回复固定文本 `CODEX_ROUTE_OK`；返回成功，并明确显示：
  - `model: gpt-5.3-codex`
  - `provider: codex`
- 当前结论：`Codex CLI + gpt-5.3-codex` 已真实跑通，可作为后续本地主力 coding agent 之一，由我直接调度执行编程任务。
- 同时确认一个工作原则：以后优先把第三方模型接到独立 coding agent（如 Codex CLI）上，尽量不要再直接改 `C:\Users\besam\.openclaw\openclaw.json` 这类 OpenClaw 系统主配置。

### 16. 今晚项目经理模式 + Codex 新员工联合作战总结（睡前日报）
- 大老板今晚明确把我的角色升级为：**真正的项目经理 / 技术总控 / 最终验收人**。
- 新工作模式确认：
  1. 我负责拆任务、派工、盯进度、做复核、最终验收；
  2. 新接入的 `Codex CLI + gpt-5.3-codex` 作为本地 coding agent / 新员工参与真实项目；
  3. 对内派工优先用英文，对大老板汇报统一用中文。
- 本轮先拿真实项目 `skills/tmall-genie-voice-bridge` 给新 Codex 做压力测试，而不是做空转 demo；目标是一边推进项目，一边验证这个新 coding agent 是否真能干活。
- 第一轮给 Codex 派工时，直接把长中文任务塞进命令行，触发参数解析错误；随后改成“任务落文件 -> 管道喂给 codex exec”，这条投喂路线更稳，后续默认沿用。
- 新 Codex 第一轮有效产出结果：
  - 能真实读项目、改文件、跑校验、补救问题；
  - 跑通 `python -m compileall -f skills/tmall-genie-voice-bridge/scripts`；
  - 先遇到 Windows / GBK 编码问题，随后自己修正 UTF-8 编码并让 `quick_validate.py` 返回 `Skill is valid!`；
  - 跑通 mock 烟雾测试：`python skills/tmall-genie-voice-bridge/scripts/speak.py ... --config config.example.json` 返回 `ok: true`。
- 第一轮产出里也暴露出管理问题：
  - 它把 `SKILL.md` 大段改成英文，改动过宽；
  - 这提醒我后续派工必须更明确：少做文案风格重写，多做可验证工程改动。
- 第二轮我重新收口任务，只让它聚焦两件事：
  1. `bridge_server` 启动 / `/health` / `/speak` / `/audio` 链路；
  2. `local_windows_speaker` 真实本机播放路径。
- 第二轮的关键工程发现与结果：
  - 它先用大段 PowerShell 一把梭测试，被策略拦截；随后改用 Python harness 分步测试，这条方式更稳；
  - 它在真实执行中踩出一个真 bug：`local_windows_speaker` 原先会把 `./scripts/play-local-audio.ps1` 相对路径绑定到“配置文件目录”，导致配置文件不在 skill 根目录时找不到脚本；
  - 它已补上低风险修复：在 `speak.py` 传入 `skill_dir`，并在 `local_windows_speaker.py` 增加“先看 config_dir，再回退到 skill_dir”的脚本解析逻辑。
- 我随后亲自做项目经理复测，确认到的状态如下：
  - ✅ mock CLI 链路通过；
  - ✅ `bridge_server` 本地链路通过：`/health` 正常、`/speak` 正常、能生成并取回音频文件；
  - ✅ `local_windows_speaker` 返回成功，`stdout` 为 `PLAYSTATE=9`，说明本机播放器流程已被成功调起；
  - ⚠️ 但这仍不能等同于“大老板肉耳已确认听到声音”；因为之后大老板明确反馈“没有听见”。
- 为了做人耳可验证测试，我又亲自跑了一轮**真人声本机测试**：
  - 把 `tts.provider` 切到 `edge`；
  - 把 backend 设为 `local_windows_speaker`；
  - 让它播报明显中文句子；
  - 结果失败，错误为：`No audio was received. Please verify that your parameters are correct.`
- 因此今晚关于“本机是否已真正出声”的最终结论必须修正为：
  - 不能说“已经确认真人语音出声”；
  - 当前只能确认：**mock 音频文件 + 本机播放器调起流程**基本通了；
  - **真人语音（Edge TTS）本机出声测试未通过**，这也是大老板没听见的直接原因。
- `tmall-genie-voice-bridge` 项目截至今晚的准状态：
  1. mock 链路：通；
  2. `bridge_server` 本地服务链路：通；
  3. `local_windows_speaker` 路径解析 bug：已找到并修补；
  4. 人耳可验证的真人语音本机出声：未通；
  5. 真实天猫精灵音箱出声：更未完成，不能夸大。
- 当前最合理的下一步（留给明天）：
  1. 排查 `edge-tts` 为什么返回 `No audio was received`；
  2. 修好后再做一次真人声本机听感测试；
  3. 只有本机真人声稳定后，再推进到真实天猫精灵 / HTTP 播放服务一侧。
- 对新 Codex 新员工的阶段性评价：
  - 不是只会说不会做，确实能干活；
  - 能踩出真 bug，也能自己补低风险修复；
  - 但需要我强约束任务边界，否则会改得过宽、文档风格漂移；
  
### 17. 网页图片点图链路已整理成独立 HOWTO
- 用户明确要求：把“网页打开后按第几行第几张图进行半自动点击”的实际用法，整理成一份独立 HOWTO。
- 已新增独立文档：`C:\Users\besam\.openclaw\workspace\WEB_IMAGE_CLICK_HOWTO.md`
- 文档内容已系统化收口，覆盖：
  - 为什么当前不优先直接用 `extensions/desktop-input-control/scripts/desktop-input.ps1`
  - 标准操作流程：`Start-Process` 打开搜索页 → `capture-screen.ps1` 截图 → `screen-ocr.py` + Tesseract 做本地 OCR → 用内联 `Add-Type + SetCursorPos + mouse_event` 点击 → 点击后再次截图 / OCR 校验
  - 当前实测沉淀的常用估位坐标
  - 已知限制、推荐汇报方式、后续优化方向
- 该 HOWTO 的定位已明确：不是“精准视觉点击系统说明书”，而是这台 Windows 机器上**已经真实验证可用**的半自动网页点图工作流。
- 相关 git 提交已完成：`fd69deb` - `Add web image click workflow HOWTO`

### 18. 每日新闻自动任务已切换为“小报告机制”
- 用户要求：把“今天发新闻的项目”改成一个**小报告**机制，用来取代原来的每天定时任务。
- 已采用最稳替换策略：
  - **不重建 Windows 任务本身**
  - 保留原有两个定时任务入口
  - 直接把背后的 `.bat` 与 Python 调用切换到新的轻量日报脚本
- 已新增脚本：`C:\Users\besam\.openclaw\workspace\daily_news_brief.py`
- 新脚本支持两个模式：
  1. `--mode comprehensive`：综合版小报告
  2. `--mode tech`：科技版小报告
- 已替换两个入口文件：
  - `C:\Users\besam\.openclaw\workspace\run_daily_comprehensive_report.bat` → 调用 `python daily_news_brief.py --mode comprehensive`
  - `C:\Users\besam\.openclaw\workspace\run_daily_report.bat` → 调用 `python daily_news_brief.py --mode tech`
- 当前新机制的目标已明确：
  - 用更轻、更快、适合每天扫一眼的小报告
  - 替代原先偏长、偏模板化的自动日报内容
- 相关 git 提交已完成：`44a541e` - `Replace daily news tasks with brief report workflow`

### 19. 新“小报告”邮件链路已真实测试成功
- 在替换完每日任务后，已立即做真实发送测试：
  - `python daily_news_brief.py --mode comprehensive` → 发送成功
  - `python daily_news_brief.py --mode tech` → 发送成功
- 用户随后又明确要求：`发一封email看一下好不好`
- 已再次实际发送一封**综合版小报告测试邮件**，并确认 SMTP 成功返回。
- 当前确认收件目标仍为：
  - `besam168168@gmail.com`
  - `758622673@qq.com`
- 终端里中文主题出现乱码属于控制台编码显示问题，不影响 SMTP 实际发信成功。
- 截至今晚，本机关于“新闻小报告自动发送”的真实状态可确认为：
  - 新脚本可用
  - 两个定时任务入口已切换
  - 手动测试发信已成功
  - 明天开始可按原有定时节奏继续自动发送“小报告版”内容

