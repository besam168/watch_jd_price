# 2026-03-31

## tmall-genie-voice-bridge 收尾记录

### 今日阶段性成果
- 以“项目经理 / 技术总控 / 最终验收”模式，并行调度两个执行助手，对 `skills/tmall-genie-voice-bridge` 做了 90 分钟强攻收尾。
- 本轮已把项目从“会说的半成品骨架”推进到“可演示 MVP”阶段，当前最稳闭环不是现场麦克风，而是：
  - `text -> local speak`
  - `text -> bridge /speak`
  - `wav -> transcribe -> echo-speak`
- 本机当前**没有物理麦克风**，因此没有继续浪费时间硬测 live mic；改为先把无麦克风演示链路、bridge 输出链路、下午接麦预案全部收好。

### 输出链路（已打通）
- 已确认旧问题根因：`local_windows_speaker` 旧链路里，`WMPlayer` 对本地 WAV 不可靠，`playState=9` 不能当真人播放成功。
- 已在 `skills/tmall-genie-voice-bridge/scripts/play-local-audio.ps1` 中为 `.wav` 增加 `System.Media.SoundPlayer.PlaySync()` 快路径，修掉“假成功 / 假失败”问题。
- 已实测通过：
  - `speak-local.ps1 -Text "你好"`
  - `scripts/speak.py --config config.local-speaker.json`
  - `bridge_server.py --config config.local-speaker.json`
  - `POST /speak` 触发本地扬声器播报
- `speak-local.ps1` 已补 UTF-8 / 中文参数处理，中文文本与 JSON 输出链路比之前稳定。
- 新增最小文本演示脚本：
  - `skills/tmall-genie-voice-bridge/demo-text-roundtrip.ps1`
  - 支持 `-Mode local` 与 `-Mode bridge`

### 输入链路（已补 MVP）
- 新增：`skills/tmall-genie-voice-bridge/scripts/listen_once.py`
- 当前支持：
  - 直接转写一个现成 WAV
  - 可选 `--echo-speak`，将识别结果回灌到现有 speak / 本地播报链路
- 已验证可用的最稳路径：
  - `wav -> 转写`
  - `wav -> 转写 -> 回播`
- 对外口径必须克制：**当前不能宣称“本机麦克风实测已打通”**，因为今天机器上没有可用物理麦克风。

### 无麦克风演示路径（已收口）
- 新增最小 helper：
  - `skills/tmall-genie-voice-bridge/demo-wav-roundtrip.ps1`
- 目前无麦克风情况下最稳的演示路径：
  1. 文本 -> 本地播报
  2. 文本 -> bridge 播报
  3. WAV -> 转写 -> 回播
- 这意味着即使下午前不接麦，也已经可以演示“类语音助手 MVP”。

### 下午接麦前预案（已准备好）
- 新增预检脚本：
  - `skills/tmall-genie-voice-bridge/scripts/check-microphone.ps1`
- 当前本机预检结论：
  - `zh-CN` recognizer 已安装
  - 默认录音设备当前不可访问
  - 与“大老板说电脑暂时没麦克风”这一现实一致
- 下午接上麦后的最短测试顺序已经明确：
  1. `check-microphone.ps1`
  2. `listen_once.py --timeout-seconds 6`
  3. `listen_once.py --timeout-seconds 6 --echo-speak --config config.local-speaker.json`
- 排障顺序也已整理到 `SKILL.md`，避免下午现场临时乱试。

### 本轮关键新增/修改
- `skills/tmall-genie-voice-bridge/scripts/play-local-audio.ps1`
- `skills/tmall-genie-voice-bridge/speak-local.ps1`
- `skills/tmall-genie-voice-bridge/scripts/listen_once.py`
- `skills/tmall-genie-voice-bridge/scripts/check-microphone.ps1`
- `skills/tmall-genie-voice-bridge/demo-text-roundtrip.ps1`
- `skills/tmall-genie-voice-bridge/demo-wav-roundtrip.ps1`
- `skills/tmall-genie-voice-bridge/SKILL.md`

### 本轮相关提交（要点）
- `69ee88f` — `Add Windows one-shot voice input MVP`
- `c15b7fe` — `Add minimal local/bridge voice demo flow`
- `86a4bd8` — `Add Windows microphone readiness checks`
- `bc6b955` — `tmall-genie: tighten no-mic demo path`

### 对外口径 / 当前最准确状态
- 可以说：
  - 本地 speaker 已打通
  - bridge `/speak` 已打通
  - 无麦克风情况下的 `wav -> 转写 -> 回播` 已可演示
  - 下午一接麦，就按预检 -> 听进去 -> 听到就说 这三步继续冲
- 不能说：
  - 本机麦克风今天已实测稳定
  - 天猫精灵本体原生麦克风已经打通
  - 已完成完整双工家庭语音助手

### 下午续做优先顺序
1. 接上物理麦克风后先跑 `check-microphone.ps1`
2. 先跑 `listen_once.py --timeout-seconds 6`，确认能拿到非空文本
3. 再跑 `--echo-speak`，确认“听到就说”
4. 若 mic 成功，则把现场实测结论补写进长期记忆与项目文档

### 全球综合报告 08:00 定时任务排查结论
- 已现场核查 `SWS_Report_Collect_0800` 与 `SWS_Report_Send_0830` 两个 Windows 任务计划：
  - `SWS_Report_Collect_0800` 上次运行时间为 **2026/3/31 08:00:01**，返回结果 **0**；
  - `SWS_Report_Send_0830` 上次运行时间为 **2026/3/31 08:30:01**，返回结果 **0**。
- 已读取 `reports/scheduled/latest_collect_status.json`：4 个抓取组 `news_core`、`markets_global`、`deep_dive`、`tech_ai_robotics` 全部成功，`failed_groups` 为空。
- 已读取 `logs/collect_comprehensive_report.log`：今天早上这轮真实抓取确实执行过，Reuters / BBC / CNBC / AP / Al Jazeera / Yahoo Finance 及市场、深挖、科技组都有成功保存记录。
- 结论：**“8:00 根本没收集”这一嫌疑基本可以排除**；更可能的问题在于：
  1. 抓到的栏目页本身就不够新；
  2. 8:30 发送链路使用的不是最新整理结果；
  3. 报告生成阶段对“只保留 24–48 小时新内容”的筛选不够严。
- 后续若继续查，应优先排：
  1. `.firecrawl/*.md` 的实际文件时间戳；
  2. `send_collected_comprehensive_report.bat` / 发送脚本到底读取哪份正文；
  3. 报告生成逻辑里“最新性过滤”是否失效。

## 2026-03-31 晚间补记：天猫精灵路线调整与新增结论
- 大老板今天明确批准一条新的技术路线：**试着进入天猫精灵后台控制权，分别推进真机发声、后台文本回调、原始收音（高风险备选）**，并允许我按 **2 小时强攻 + 两个助手并行写代码 + 我做总指挥** 的方式推进。
- 今天晚些时候还再次验证了一个重要现实：**本地电脑扬声器“脚本成功”不等于“人耳真实听到”**。虽然 `speak-local.ps1` / `demo-text-roundtrip.ps1 -Mode bridge` 返回过 `ok=true`、`PLAYBACK_CONFIRMED=1`、`PLAYSTATE_FINAL=WAV_SYNC_OK`，但大老板现场反馈是 **“什么声音都没有”**。因此以后对外口径必须更严格：
  - 只能说“程序链路跑通”；
  - **不能**再把 `SoundPlayer` 返回成功直接说成“已经真实出声成功”。
- 因此，当前对本机播报的最准确表述应升级为：**程序层可跑，但未取得现场人耳验收，不能当成真实出声打通。**
- 今天已确认 bridge `/speak` 命令层面能触发，且 `demo-text-roundtrip.ps1 -Mode bridge` 可走通 **bridge -> local_windows_speaker**；但这仍然**不是**天猫精灵实体设备发声，只是桥接入口打到了本机后端。
- 两小时强攻中，至少一位执行助手已产出关键有效成果：为 `skills/tmall-genie-voice-bridge/scripts/bridge_server.py` 增加了更贴近真实平台的**文本回调入口**，并已提交：
  - 新增接口：`POST /callback/text`、`POST /webhook/text`
  - 支持提取字段：`text`、`query`、`utterance`、`message`、`payload.text`、`payload.query`、`intent.query`、`request.text`、`request.query`
  - 已同步补充 `SKILL.md` 文档说明
  - 提交：`a06fc50f78c04d5118052ae630643089a2c4b316` — `Add text callback webhook route for Tmall bridge`
- 这条新增意味着：**“后台文本回调路线”已经从概念变成当前最现实、最值得继续推进的主线。** 现在最可行的结构是：
  1. 上游技能/自动化平台把识别后的文本回调给本地 bridge；
  2. bridge 提取文本；
  3. 再交给 `speak.py` / 播报后端。
- 当前三条路线的优先级已更清楚：
  1. **后台文本回调路线**：最可行，已增强并做过 smoke test；
  2. **技能 / webhook 回调路线**：可行，但要看外部平台是否允许自定义 webhook、是否提供 query / intent / session 字段、是否要求签名校验；
  3. **原始收音路线**：高风险，当前没有任何设备协议、开放接口或抓包证据支撑，不能对外宣称已接近打通。
- 目前最务实的下一步不是继续幻想“直接抢天猫精灵原始麦克风”，而是：**尽快拿到真实上游 webhook / skill payload 样例，再按样例补签名校验、字段映射和 session/trace 追踪。**
- 今晚收口结论进一步明确：项目**还未完成真机闭环**，但关键桥接层已进入可继续对接阶段：
  1. 文本回调层已补上：`POST /callback/text`、`POST /webhook/text`；
  2. 真机播放前置条件已补上：支持自动或显式公开音频 URL（`audio_base_url = "auto"` / `public_base_url`）；
  3. 当前最准确项目状态应写成：**桥接层基本成形，只差真实设备控制端接最后一跳**；
  4. 明日默认优先级固定为：**Home Assistant -> 阿里技能/云函数/官方开放平台 -> 第三方控制接口**；原始收音路线继续压成高风险备选。 