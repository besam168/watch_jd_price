# 2026-05-14

- 今晚正式启动并推进 `coding-office` 项目，目标是让 OpenClaw 能安全调用本地 Coding Agent（Claude CLI / Codex CLI / 后续 Tanzo），形成“OpenClaw = 总经理 / 调度员，CLI Agent = 程序员，MCP / Plugin = 调度系统”的本地开发调度框架。
- 当前项目定位已经明确：
  - 只做：`OpenClaw -> 本地 Coding Agent -> 返回结果`
  - 不做：阿三语音主线、不做复杂远程编排、不做高危系统自动化
- 今晚已落地框架文件：
  - `skills/coding-office/SKILL.md`
  - `skills/coding-office/README.md`
  - `skills/coding-office/OPENCLAW_CODING_OFFICE_SPEC.md`
  - `skills/coding-office/HANDOFF_TEMPLATE.md`
- 已补成一份正式交接口径：`skills/coding-office/PROJECT_HANDOFF.md`（由大老板口述确认）
- 当前 `coding-office` 项目框架、文档、配置样例、JSON schema 与调用样例已全部落地；`config/`、`schemas/`、`examples/` 的 JSON 文件已在本机通过 parse 验证。
- 今晚真实接通测试已推进到 **MCP 注册层打通**，当前确认结果如下：
  1. `openclaw --version` 正常，版本为 `2026.4.2`
  2. `openclaw plugins --help` 正常
  3. `openclaw mcp --help` 正常
  4. `claude --version` 正常，返回 `2.1.119 (Claude Code)`
  5. `codex --version` 正常，返回 `codex-cli 0.117.0`
  6. 已成功安装插件：`clawhub:openclaw-safe-agent-cli-mcp`
  7. 插件 `safe-agent-cli-mcp` 已启用、已加载、已激活
  8. 插件自带两个 MCP server：
     - `safe-claude`
     - `safe-codex`
  9. 今晚踩实了一个关键兼容点：
     - 错误写法：根层 `mcpServers`
     - 正确写法：`mcp.servers`
  10. 修正后，`openclaw mcp list` 已能正常列出：
      - `safe-claude`
      - `safe-codex`
  11. `openclaw mcp show safe-claude --json` 与 `openclaw mcp show safe-codex --json` 都已正常返回 command / args / cwd
- 当前最准确项目状态：
  - **框架层**：已完成并可交付
  - **插件安装与加载层**：**仅元数据层完成**（插件已安装、已启用、已加载）
  - **MCP 注册层**：已真实打通
  - **插件执行层**：**未打通**（当前安装目录缺少 `packages/.../dist/index.js` 与 workspaces 内容，属于不完整源码 bundle，暂不能真实执行 `safe-claude` / `safe-codex`）
  - **功能工具层**（`claude_status / codex_status / claude_config / codex_config / review / validate / task-dry-run`）：**还没全部跑完，不可宣称已完全测通**
- 当前新增实测结论：
  1. `claude --version` 已返回 `2.1.119 (Claude Code)`
  2. `codex --version` 已返回 `codex-cli 0.117.0`
  3. `skills/coding-office/config/`、`schemas/`、`examples/` 下全部 JSON 文件已 parse 通过
  4. `C:\Users\besam\.openclaw\extensions\safe-agent-cli-mcp` 当前只有 `.mcp.json`、`README.md`、`package.json`、`plugin/`、`.claude-plugin/` 等壳文件，缺少 README 所要求的 `packages/claude-cli-mcp/dist/index.js` 与 `packages/codex-cli-mcp/dist/index.js`
  5. 因此当前应把这个插件判断为：**注册成功，但执行层安装不完整**
- 当前对外口径必须克制：
  - 可以说：`coding-office` 第一阶段框架 + 插件元数据安装 + MCP 注册已打通
  - 可以说：Claude/Codex 本机 CLI 本体可用
  - 不能说：`safe-agent-cli-mcp` 已可真实执行 Claude/Codex 的 MCP 工具调用
  - 不能说：Claude/Codex 全链路工具调用已经全部测试完成
- 下一步固定任务：
  1. 修复 `safe-agent-cli-mcp` 安装不完整问题（重装完整源码包或补 build）
  2. 修复后继续做 `status / config / review / validate` 实测
  3. 若通过，再做 `task dry-run`
  4. 真写入任务必须等大老板确认后再测
