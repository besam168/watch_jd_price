# 2026-04-03

## 长期记忆 / memory_search 排障记录

- 用户反馈：长期记忆“读不出”，要求排查 OpenClaw 的长期记忆检索能力。
- 已确认：工作区中的 `MEMORY.md` 与 `memory/` 目录存在，不是记忆文件丢失。
- 已确认：OpenClaw 服务可运行，但最初 `openclaw memory status` 显示 `Memory: 0 files / 0 chunks`，说明索引层未正常建立。
- 初始阶段排查结果：
  - `memory_search` 报 `embedding/provider error`、`fetch failed`
  - 自动选择的远程 embedding 路线不可用
- 已尝试将 `agents.defaults.memorySearch.provider` 从自动/远程路线切换：
  1. 切到 `gemini`
  2. 重启 OpenClaw
  3. 再次索引
- Gemini 路线结果：
  - 配置切换生效，但 `openclaw memory status --deep --index --verbose` 仍报 `fetch failed`
  - 进一步测试表明当前机器到 Gemini embedding 链路不可用，不适合作为长期记忆 embedding 来源
- 已测试 OpenAI 兼容 embeddings 备选：
  - 现有常用上游不支持 `/v1/embeddings`，无法用于长期记忆
  - 用户提供的 `https://fastai.fast` 也返回 404，当前同样不能直接用于 OpenClaw memory embeddings
- 因远程 embedding 路线均不可用，已改为本地 embedding 方案：
  - 将 `C:\Users\besam\.openclaw\openclaw.json` 中 `agents.defaults.memorySearch` 改为：
    - `provider = "local"`
    - `fallback = "none"`
    - `local.modelPath = "hf:ggml-org/embeddinggemma-300m-qat-q8_0-GGUF/embeddinggemma-300m-qat-Q8_0.gguf"`
- 本地 embedding 路线当前状态：
  - OpenClaw 已识别到 `provider: local`
  - 但索引失败，核心错误为：`node-llama-cpp is missing`
- 为修复本地依赖，已执行：
  - `npm i -g openclaw@latest`
  - `npm i -g node-llama-cpp`
- 当前实际结果：
  - OpenClaw 仍提示 `node-llama-cpp` 缺失或不可用
  - 说明依赖虽然尝试安装，但还没有进入 OpenClaw 实际可加载的模块路径
- 当前结论：
  - 不是记忆文件丢失
  - 不是单纯配置写错
  - 不是单纯重启问题
  - 当前真正卡点是：**本地 embeddings 依赖 `node-llama-cpp` 仍未被 OpenClaw 正常加载**
- 后续建议动作：
  1. 继续硬修 `node-llama-cpp` 的安装路径与 OpenClaw 模块加载路径
  2. 必要时对 OpenClaw 全局安装目录做保守级重装/重建
  3. 修复后重新执行：
     - `openclaw memory status --deep --index --verbose`
     - `openclaw memory search --query "大老板" --max-results 3`
  4. 通过后再确认 `memory_search` 在真实对话里恢复可用

## 与用户沟通中的关键说明

- 已明确告知用户：这次问题不是 `MEMORY.md` 没了，而是长期记忆的 embedding / 索引链路坏了。
- 已明确告知用户：继续“硬修”主要会动 OpenClaw 的安装层和依赖，不会默认乱改业务脚本、记忆内容和频道配置。
- 用户已同意：
  - 改用本地 embedding 模型
  - 继续安装依赖
  - 将这次工作记录到工作区

## 当前状态标签

- 状态：**未修复完成，已定位到依赖加载层**
- 优先级：高
- 下一步：继续排 `node-llama-cpp` 与 OpenClaw 全局模块路径

---

## tmall-genie-voice-bridge / local-whisper 进展（2026-04-03）

- 目标：把 `tmall-genie-voice-bridge` 从不稳定的 Windows `System.Speech` 中文识别，升级到本地 Whisper 路线，并打通 `mic -> wav -> whisper -> speak`。
- 已确认硬件与播放层没问题：
  - `Logi C270 HD WebCam` 麦克风可见、可录
  - 本地播报 `speak-local` / `local_windows_speaker` 正常，曾多次返回 `PLAYBACK_CONFIRMED=1` 与 `PLAYSTATE_FINAL=OK`
- 已安装并打通 `skills/local-whisper`：
  - 建立独立 venv：`skills/local-whisper/.venv`
  - 安装完成：`torch`、`openai-whisper`、`imageio-ffmpeg`
  - 本地 ffmpeg 通过 `imageio-ffmpeg` 补齐，并复制为标准名 `ffmpeg.exe` 供 Whisper 子进程调用
- 已验证 `wav -> whisper` 真转写：
  - 使用 `tmp_audio/listen-once-test.wav`
  - `base` 模型能真实返回中文文本，如：`你好神萬三`
- 已把 `local_whisper` 作为新引擎接回：
  - 文件：`skills/tmall-genie-voice-bridge/scripts/listen_once.py`
  - 新增 `--engine local_whisper`
  - 支持 `--wav` 路线直接走本地 Whisper
- 已进一步打通：
  - `wav -> whisper -> speak`
  - `mic -> wav -> whisper -> speak`
- 已新增麦克风录音路径：
  - 用 ffmpeg dshow 从 `麦克风 (Logi C270 HD WebCam)` 录成临时 wav
  - 再交给 local Whisper 转写
- 已新增调试能力：
  - `--keep-recorded-wav`
  - `--pre-roll-seconds`
  - 方便保留录音样本、分析抢拍/截断/串音问题
- 关于模型实测结论：
  - `small` 模型下载时曾出现损坏，后已通过断点重试修复，并成功 `SMALL_LOAD_OK`
  - 但在这台机器的现场麦克风短句测试中，`small` **不比 `base` 更稳，反而更差**
  - `base` 在多轮实际短句里至少能部分命中，如：
    - `我听到了` -> 正确识别
    - `今天继续测试` -> 识别为 `今天繼續測試`（繁体可接受）
  - `small` 在 4 轮短句对照中明显跑偏，例如识别成：
    - `其他魚也一起隨便吧吃吃吧`
    - `炒水`
    - `grill`
    - `字幕by索兰娅`
- 当前阶段结论：
  - **工程链路已经打通，不再是“能不能跑”的问题**
  - 当前主要瓶颈已经收敛为：
    - 现场录音质量
    - 电脑播报串入麦克风
    - 说话节奏 / 抢拍
    - Whisper 在当前样本下的精度
- 当前更稳的现场默认建议：
  - 现场麦克风测试优先使用 `--whisper-model base`
  - 再配合 `--keep-recorded-wav --pre-roll-seconds 1.5` 做可回放诊断
- 今天为这条线新增并落地的关键提交：
  1. `943e634` - `Add local Whisper wav transcription path`
  2. `ae8d56a` - `Add microphone capture path for local Whisper`
  3. `52e96a4` - `Default local Whisper model to small`
  4. `5154ff4` - `Add mic capture debug options for Whisper`
- 今日最终口径：
  - `tmall-genie-voice-bridge` 已完成本地 Whisper 闭环 MVP（`mic -> wav -> whisper -> speak`）
  - 但若追求可日常稳定使用，还需要继续做录音/节奏/回声与模型精度调优，当前不宜夸大成“稳定语音助手已完成”
- 今晚又推进了一步：
  - 已新增 `skills/tmall-genie-voice-bridge/scripts/wake_loop.py`，用于持续短轮询监听 + 唤醒词触发本地播报
  - 先后试过 `沈万三`、`沈万三在吗` 两套唤醒词，现场命中率都不稳；说明问题不只是匹配规则，更在前端收音与 Whisper 对短词/短句的现场识别稳定性
  - 后续把唤醒词改成 **`阿三，在吗`**，并加入近音/误识别容错（如 `阿三/啊三/阿散/阿山/阿桑 + 在吗/在嗎/在麻/在嘛/在么` 等）后，首次取得真实成功触发
  - 这次成功样本里，Whisper 实际识别文本并不标准，而是：`rainbow啊! 散散!在嗎??`，但因为容错匹配已覆盖到 `啊/阿 + 散/三 + 在嗎` 这一类变体，仍然成功判定命中
  - 命中后已真实自动播报：**`大老板，我在`**，并返回 `PLAYBACK_CONFIRMED=1` / `PLAYSTATE_FINAL=OK`
  - 后续又把完整链路推进到：**`阿三，在吗` -> `在` -> 直接监听下一句问题**，不再额外说“请说”，以减少等待时间
  - 这条提速版整链已经真实跑通多轮：唤醒后可静默播报 `在`，并继续收下一句问题；虽然问题识别精度仍会漂，但流程层已经打通
  - 本地播放层今晚也完成重要修正：已不再走会弹出播放器窗口的 mp3 默认程序路线，而是改为 **edge-tts 先生成音频 -> ffmpeg 转真正 wav -> `SoundPlayer.PlaySync()` 静默播放**
  - `config.local-speaker.json` 已切到 `output_ext = "wav"`，静默播放真测成功，返回 `PLAYBACK_BACKEND=SoundPlayer` / `PLAYSTATE_FINAL=WAV_SYNC_OK`
  - 今晚还已安装并初步调查现成方案 `percept-voice-cmd`：
    - 安装目录：`skills/percept-voice-cmd`
    - 结论：这是目前查到最像“唤醒词 + 语音命令路由”的现成 skill，但它依赖 `percept-listen` 与整套 Percept 体系，不是单装一个 skill 就能直接替换当前原型
  - 当前更准确项目状态应更新为：
    - **本地 Whisper + 静默播报 + 容错唤醒 + 唤醒后继续听一句** 的原型链路已打通
    - 但仍未达到稳定高命中率、低延迟、可日常使用的正式版
- 今晚这条线新增提交：
  5. `66afffc` - `Add wake phrase loop prototype`
  6. `e17bb9c` - `Add fuzzy wake matching for Shen Wansan`
  7. `522801f` - `Use longer fuzzy wake phrase for voice loop`
  8. `4fc9f65` - `Switch wake phrase to A-San zai ma`
  9. `465e508` - `Add follow-up question listen after wake`
  10. `a410624` - `Enable silent wav playback for local speaker`
  11. `8ac1d45` - `Set local speaker config to wav output`
  12. `c8e2b16` - `Speed up wake flow by removing extra prompt`

---

## desktop-input-control / 中文 OCR / QQ 自动化进展（2026-04-03）

- 目标一：继续排查并增强 `extensions/desktop-input-control`，重点验证它对真实中文桌面截图的识别能力。
- 已确认目标项目就是：
  - `C:\Users\besam\.openclaw\workspace\extensions\desktop-input-control`
- 已梳理项目状态：
  - 插件 id：`desktop-input-control`
  - 主文件：`index.ts`、`README.md`、`HOWTO.md`、`scripts\desktop-input.py`
  - 最近关键提交集中在窗口锁定、焦点确认、点击诊断、桌面自动化流程升级
- OCR 方向已查明旧问题来源：
  - 机器上最初 `tesseract` 不在 PATH
  - 系统缺少 `chi_sim.traineddata`
  - 脚本默认更容易走到 RapidOCR，而它在中文桌面截图上效果差
  - Windows 控制台输出 JSON 时还会被 `gbk` 编码炸掉
- 已完成的 OCR 修补：
  - 给 `screen-ocr.py` 增加 `--engine auto|rapidocr|tesseract`
  - 增加插件内置 tessdata 自动发现与配置
  - 修正 `chi_sim+eng` 语言串的识别与回退逻辑
  - 增加 UTF-8 安全 JSON 输出 `safe_print_json(...)`
  - 增加 `--psm`，并把 Tesseract PSM 自动选择逻辑写入脚本
- 已在插件内置目录落地中文 OCR 依赖：
  - `extensions\desktop-input-control\third_party\tessdata\chi_sim.traineddata`
  - `eng.traineddata`
  - `osd.traineddata`
- 已完成真实截图验证，不只是看代码：
  - 在插件示例截图与主屏实时截图上都反复测试过
  - 已确认插件现在**可以识别主屏中文内容**，识别结果里能稳定看到如：
    - `东方财富`
    - `上证指数`
    - `深证成指`
    - `OpenClaw`
- 已做参数基准测试，并得到当前机器上更稳的中文桌面默认：
  - preprocess：`gray_upscale2x`
  - 大范围中文桌面截图默认 `psm=12`
  - 小区域中文默认更适合 `psm=6`
- 已产出并提交两次关键修复：
  1. `16d49f6` - `Improve desktop OCR Chinese detection path`
  2. `ce60713` - `Tune desktop OCR defaults for Chinese screens`
- 对 QQ 桌面自动化的真实测试结论：
  - 已确认 QQ 窗口可以找到、聚焦、切前台、点击、输入中文
  - 用户已确认一次实际发送的 `你好` 本身并未乱码，说明**中文输入链路可用**
  - 但当前真正没打通的不是“能不能发中文”，而是：
    1. 搜索框焦点无法稳定确认
    2. 左侧搜索结果区无法稳定确认出现目标联系人 `新干线`
- 已明确一个重要方法论：
  - **不能把整窗 OCR 文本里出现“新干线”，等同于真的在左侧联系人结果区里定位到了“新干线”**
  - 后续必须只看 QQ 左侧搜索结果区，且最好拿到可点击坐标，才能宣称找到目标
- 已完成一次更干净的 QQ 主屏重测：
  - 确认 QQ 在主屏前台
  - 点击蓝色标题栏
  - 再点估算的搜索框区域
  - 直接输入 `新干线`
  - 只截左侧结果区复核
- 该次重测最终结果：
  - 左侧结果区 OCR 仍未稳定出现 `新干线`
  - 因此没有继续发送 `今天天气好吗`
- 当前阶段结论：
  - `desktop-input-control` 的**中文 OCR 能力已从“基本不可用”提升到“对真实中文桌面可用”**
  - 但在 QQ 这种复杂 UI 中，要实现稳态自动化，还需要继续做：
    - 搜索框焦点确认
    - 左上搜索区小区域 OCR
    - 左侧结果区定向 OCR / 命中判断
    - 必要时加 QQ 专用 search-state 检测逻辑

---

## 协作报告定时任务处理（2026-04-03）

- 用户要求：把《协作报告》这个“每 10 分钟”自动发一次的任务完全关掉。
- 已定位到真实 cron 任务：
  - 名称：`协作报告_每10分钟`
  - id：`a816c3a3-adeb-4956-9344-5f037c35fbc3`
  - schedule：`everyMs = 600000`
  - 投递渠道：`qqbot:c2c:A79F990232234F712BD31B9E2FF973F6`
- 已执行关闭：
  - `openclaw cron disable a816c3a3-adeb-4956-9344-5f037c35fbc3`
- 关闭后复核结果：
  - 返回 `enabled: false`
  - `openclaw cron list` 显示 `No cron jobs.`
- 当前结论：
  - 这个“每 10 分钟自动发《协作报告》”的 cron 已完全停掉，后续不会再自动发。

## 补充记录（pre-compaction flush）

- 今天已按大老板要求，把本轮项目工作总结写入 `memory/2026-04-03.md`，作为后续续工入口。
- `desktop-input-control` 这条线，今日阶段性结论要长期记住：
  - 中文 OCR 已从“基本不可用”修到“主屏中文可识别、可用于真实桌面验证”；
  - 但在 QQ 这种复杂聊天 UI 上，还**不能宣称已稳定可用**；
  - 当前真正卡点不是中文发送能力，而是 **QQ 搜索框焦点确认** 与 **左侧联系人结果区（如“新干线”）的可靠定位**。
- 今日 QQ 自动化的安全口径继续保持：
  - 只有在左侧结果区真正识别并定位到目标联系人后，才允许继续点击并发送消息；
  - 不能因为全窗 OCR 某处出现“新干线”字样，就当成已定位成功。
- 今日用户明确要求：把本轮工作“总结一下，放到记忆区”。这说明以后做较长排障/工程任务，收尾时应主动整理进当日 `memory/YYYY-MM-DD.md`。
- 今日已关闭一个高频打扰型 cron：
  - 名称：`协作报告_每10分钟`
  - 结论：已禁用，不再作为默认周期汇报来源。

---

## 长期记忆硬修续进展（晚间追加，2026-04-03）

- 晚间继续推进 OpenClaw 长期记忆硬修，用户已明确授权：可以按“硬修”方式继续处理安装层 / 依赖层 / gateway 重启，但不默认改业务脚本与记忆原文。
- 已确认一个关键事实：
  - `node-llama-cpp` **其实已经装在全局 npm 目录**，不是完全没装。
- 已找到更准确的阻塞点：
  - 问题不再只是“没安装”，而是 **OpenClaw 自己没有从正确模块路径解析到 `node-llama-cpp`**。
- 已完成一次关键硬修动作：
  - 在 `C:\Users\besam\AppData\Roaming\npm\node_modules\openclaw\node_modules\` 下，为全局 `node-llama-cpp` 补了一个可见入口（junction）
  - 这一步后，已通过模块解析验证：**OpenClaw 进程已能 resolve 到 `node-llama-cpp`**。
- 这意味着：
  - 之前“本地 embedding 引擎根本起不来”的核心阻塞，已被推进一大步；
  - 故障层级已从“依赖缺失/模块不可见”推进到“索引执行层”。
- 用户随后手动重启了电脑 / OpenClaw 环境，用于让新的依赖解析状态真正生效。
- 重启后进一步验收发现：
  - `openclaw memory search --query "大老板" --max-results 3` 仍未恢复到可用结果；
  - `openclaw memory index --force --verbose` 与相关 `memory status` 命令在执行中多次被 **SIGKILL**；
  - 因此当前**还不能宣称长期记忆已修好**。
- 晚间最新阶段性判断：
  - 现在的主要问题已经不是文件层、不是纯配置层、也不是单纯 `node-llama-cpp` 缺失；
  - 当前故障已收敛为：**memory indexing / 落库前执行阶段被杀**。
- 已确认的辅助线索：
  - `.openclaw\memory\main.sqlite` 存在，但时间较老，没有随着这轮修复明显更新；
  - `.openclaw\logs\` 下暂未拿到足够直接的 memory 崩溃明文日志；
  - 说明索引流程大概率在真正稳定写库之前就被打断。
- 还要长期记住一个排障教训：
  - 晚间一次全局搜索把工作区里的 `torch` 安装包二进制内容、以及 `memory/2026-04-03.md` 文本本身，也误命中成了“embedding 线索”；
  - 以后排这种问题，**必须避免把工作区二进制包内容和自己的工作记录误当成 OpenClaw 真日志**。
- 当前最准确项目状态：
  - **依赖解析层已推进**（`node-llama-cpp` 可见性问题基本已突破）
  - **索引执行层未修通**（`memory index` / `memory status` 相关流程被 SIGKILL，且未稳定写入 sqlite）
- 下一步优先级已固定为两条：
  1. 查更直接的 **memory / gateway 实时日志**，拿到进程被杀前最后一条硬信息；
  2. 把 indexing 动作拆成更小颗粒，判断究竟死在：
     - 文件扫描阶段
     - embedding 初始化阶段
     - 还是落库前阶段
- 对外口径必须保持克制：
  - 可以说：**长期记忆硬修已从“依赖层”推进到“索引执行层”**；
  - 不能说：**`memory_search` 已恢复**；
  - 不能说：**根因已经完全查明**。
