# 2026-05-15

- 今日继续推进 `coding-office` 项目，最终确认一个关键结论：**OpenClaw 当前系统内本来就已经暴露了原生 `safe-claude__*` / `safe-codex__*` 工具，可直接调用 Claude Code / Codex CLI。**
- 这意味着从实际使用角度：
  - `coding-agent` skill 更偏“后台 coding agent 调度方式”；
  - `safe-claude` / `safe-codex` 更偏“系统原生本地 CLI 工具接口”；
  - 因此不是只有 `coding-agent` 才能用 Claude/Codex，系统本身就能直接调。
- 今日已做的真实验收：
  1. `claude --version` 返回 `2.1.119 (Claude Code)`；
  2. `codex --version` 返回 `codex-cli 0.117.0`；
  3. 原生 safe-* 工具的 `validate` / `review(dryRun)` / `task(dryRun)` 全部通过；
  4. 说明命令拼装、白名单、只读保护、写入 dry-run 门禁都正常。
- 当前仍需保留的边界：
  - 原生 safe-* 工具的 `status` / `config` 在 Windows 下对 `claude` / `codex` 的路径发现仍有点别扭，显示 `ENOENT`；
  - 但这与本机 CLI 实际不可用不是一回事，因为直接跑版本命令是通的；
  - 所以应表述为：**核心可用，状态探测层还有 Windows 路径发现小问题。**
- 另一个关键结论：ClawHub 上的 `openclaw-safe-agent-cli-mcp` 发布包本身不完整；已通过 GitHub 源码仓库 `udaymanish6/openclaw-safe-agent-cli-mcp` 本地 build 补齐扩展目录，确认 `dist/index.js` 已落地。
- 已把“系统原生可直接调 Claude/Codex”的结论补写进 `skills/coding-office/PROJECT_HANDOFF.md`，作为后续交接依据。
