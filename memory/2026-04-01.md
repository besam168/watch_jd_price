# 2026-04-01

## tmall-genie-voice-bridge 项目进展（重启前工作记录）

- 今天继续沿着 `skills/tmall-genie-voice-bridge` 往“真实接入可验收”收口。
- 已在正式项目内连续提交：
  - `c0f4a04` — Add real HTTP player rehearsal runner
  - `0d10bc5` — Tighten Home Assistant preflight checks
  - `72b91e1` — Add Home Assistant acceptance checklist
- 当前正式项目已具备：
  - 真实 HTTP 播放预检（preflight）
  - rehearsal 演练链
  - Home Assistant 真实接入验收清单
- 本机播放链路今天拿到真实新证据：
  - 使用 `config.local-speaker.json` 跑 `demo-text-roundtrip.ps1`
  - 返回 `PLAYBACK_CONFIRMED=1`
  - 返回 `PLAYBACK_BACKEND=SoundPlayer`
  - 返回 `PLAYSTATE_FINAL=WAV_SYNC_OK`
  - 大老板人耳反馈：听到了“du”一声
  - 结论：**本机本地播放动作已确认触发**，但由于当前 `config.local-speaker.json` 的 `tts.provider` 仍为 `mock`，**这不能算本机真人中文语音已确认打通**。
- 麦克风链路今天也拿到真实新证据：
  - `scripts/check-microphone.ps1` 返回 `default_input_accessible=true`
  - 已识别到摄像头麦克风设备：`Logi C270 HD WebCam`
  - `zh-CN` recognizer 显示可用
  - 多次运行 `python .\\scripts\\listen_once.py --timeout-seconds 6/8 --culture zh-CN`
  - 结果表现为：有时抓到乱码/低置信度文本（接近“打开灯”轮廓），有时直接超时 `No speech recognized before timeout.`
  - 当前结论：**麦克风硬件已接通、System.Speech 中文识别链路不是死的，但实时中文识别仍不稳定，不能宣称本机听说闭环已打通。**
- 按大老板要求，已真实拉起 Codex CLI 作为执行层，不再只靠我单线程推进。
- Codex 工作方式：
  - 在临时副本仓库中运行，避免污染正式项目：
    - `C:\Users\besam\AppData\Local\Temp\tmall-genie-codex-20260401-170435`
  - 两轮后台 Codex CLI 最终都被系统 SIGKILL，但**不是白跑**；临时副本里已经留下实质改动。
- 当前已确认 Codex 在临时副本中改动了这些文件：
  - `scripts/check-microphone.ps1`
  - `scripts/listen_once.py`
  - `scripts/record_acceptance_result.py`
  - `tests/test_acceptance_recorder.py`
  - `README.md`
  - `HOME_ASSISTANT_ACCEPTANCE.md`
- 其中“验收结果落盘器”这条线已拿到专项测试通过证据：
  - 命令：`python -m unittest tests.test_acceptance_recorder -v`
  - 结果：`6/6 OK`
  - 说明：已能记录 config / preflight / rehearsal / human confirmation，并生成 JSON / Markdown 验收记录。
- 临时副本里的 `listen_once.py` 已被增强，方向包括：
  - PowerShell 结果改为 base64 包 JSON，减少中文乱码/控制台编码问题
  - 增加 silence / babble / end-silence 等参数
  - 增加多次尝试与 best attempt 选择
  - 增加更诚实的 warning，明确 `System.Speech` 中文识别只是 best-effort
- **重启前的最重要待办 / 下一步：**
  1. 电脑重启后，先检查正式项目目录与临时副本目录是否还在；
  2. 先读取并审查临时副本这几处改动；
  3. 将合格改动挑回正式项目 `skills/tmall-genie-voice-bridge`；
  4. 在正式项目里重新跑：
     - `python -m unittest tests.test_acceptance_recorder -v`
     - 视情况再跑完整测试集；
  5. git commit 正式项目改动；
  6. 再继续判断本地 STT 是否值得保留为主线；如果仍不稳，应改为“录 WAV 再转写”或更稳识别方案，不再死磕当前实时 `System.Speech`。
- **当前对外诚实口径必须保持：**
  - 已确认：本机播放器动作通、电脑能发出声音；
  - 已确认：麦克风被系统识别，收音链路不是死的；
  - 已确认：验收结果落盘器已在临时副本里过专项测试；
  - 未确认：本机真人中文 TTS 稳定播报；
  - 未确认：本机“听说闭环”稳定打通；
  - 未确认：真实 Home Assistant / 真天猫精灵硬件播放已完成。

## desktop-input-control 项目进展（重启前补记）

- 今天继续推进 `extensions/desktop-input-control`，方向从“能动”继续收口到“更稳、更可控、可回放”。
- 已新增并提交：
  - `8a44cf8` — `Add desktop automation phase 3 reliability features`
  - `9ef7ecd` — `Add window lock and demo workflow for desktop automation`
- 本轮已落地能力：
  - 窗口列表、按标题/ PID 聚焦
  - 点击重试
  - 点击前后截图归档到 `artifacts/`
  - 窗口锁：`get/set/clear`
  - `scripts/demo-workflow.ps1`
  - `.gitignore`，避免把 logs / artifacts / node_modules / 临时 png 带进提交
- 本轮真实验收结论：
  - `npm run typecheck` 通过
  - 窗口锁机制本身已生效
  - 截图归档已真实落盘
  - 但完整 demo 暴露了一个真问题：锁窗目标选择仍可能误锁到 `.openclaw - 文件资源管理器`，导致后续 `type-text` / `press-hotkey` 被护栏正确拦截
- 因此当前最准确口径应为：
  - **三期/四期骨架已成形**
  - **窗口锁机制是真的**
  - **但“锁谁”这一步还没完全收干净，需要继续精修**
- 重启后下一步固定优先级：
  1. 优先锁当前前台窗口
  2. `set-window-lock` 支持 foreground 模式
  3. PID + 标题双重确认
  4. `demo-workflow.ps1` 默认锁当前前台窗口，不再依赖模糊匹配猜目标
- 大老板今天还明确授权：后续工程任务可以由我做总指挥 / 设计 / 最终验收，并按需调 `Claude Code` 做代码执行助手；但最终口径、验收结论与边界仍由我统一负责。
- 2026-04-01 晚间，大老板进一步把 **`tmall-genie-voice-bridge` / 天猫精灵项目** 的协作模式正式定死：
  - 由我担任 **总指挥 / 方案设计 / 最终验收 / 对外统一口径负责人**；
  - 允许我默认调 **Codex CLI** 作为执行助手参与写代码、跑验证、补文档；
  - 但执行层输出不能直接当最终结论，必须由我复核、收口、验收后再对外汇报；
  - 默认按“全部权限给我推进，但口径和边界由我负责”执行。
- 2026-04-01 夜间，`extensions/desktop-input-control` 又继续往稳态版收口，并新增提交：
  - `6404600` 曾一度混入 `tmall-genie-voice-bridge` 文件，随后已按方案 A 本地清洗并改写为干净提交：`9e56846` — `Prefer foreground window for safer desktop locks`
  - `8670217` — `Add locked input workflow for desktop automation`
  - `1bca8a4` — `Upgrade desktop automation workflow and click diagnostics`
  - `daa43b9` — `Verify desktop focus transitions before input`
- 当前 `desktop-input-control` 已确认新增并可用的关键能力：
  - `foreground` 模式锁窗（优先锁当前前台窗口）
  - `get-foreground-window-info`
  - 更稳的锁窗判定：优先 `hwnd`，再结合 `pid/title`
  - `desktop_run_locked_input_flow`
  - `desktop_click_text_on_screen` 已支持 `lockForeground` / `lockWindowTitle` / `lockWindowPid` / `clearLockAfter`
  - 完整点击演示脚本：`scripts/demo-locked-click-flow.ps1`
  - 新增 `desktop_focus_window_verified` / `focus-window-verified`：支持 focus 重试、focus 后复核、返回 `expected / actual / attempts`
- 当前对 `desktop-input-control` 的最准确工程判断：
  - 锁窗、输入、热键、OCR dry-run、真点击、截图归档、动作回放都已经形成闭环；
  - **Windows 前台窗口切换是否真正成功，已经能被显式验证和记录，不再靠猜。**
  - 真实验收里已出现很有代表性的场景：`focus-window-verified chrome 0 2 250` 明确返回 `focus verification failed`，但完整 demo 仍能继续通过 OCR 命中 + 真点击把前台切到 `OpenClaw Control - Google Chrome`；这说明“focus 失败”和“后续点击成功”现在已经能被同时看见并写进结果。
  - 因此当前下一步最值得继续收的是：**前台控制策略升级**，包括 focus 多策略、将 `focusStrategy` 写进回包，以及让输入/点击工作流支持 `requireFocusSuccess=true`，由上层决定 focus 失败后是否直接中止。

## 2026-04-01 夜间补记：天猫精灵项目路线收口

- 今晚继续真实排查 `tmall-genie-voice-bridge` 的真机最后一跳，没有偷懒靠旧记忆，实际做了这些核查：
  - 检查 `config.json` / `config.home-assistant.example.json` / `config.real-http-player.template.json`；
  - 对当前 `config.json` 跑 preflight，确认卡点为：
    - `backend.type` 仍是 `mock_tmall_genie`；
    - `Authorization` 仍是占位符 `Bearer REPLACE_ME`；
    - bridge `/health` 当时不可达；
    - `entity_id` 仍是示例值 `media_player.tmall_genie`；
  - 实测本机局域网 IP 为 `192.168.2.222`，因此 bridge 对局域网可访问地址可先定为 `http://192.168.2.222:57881`；
  - 扫描 `192.168.2.0/24` 常见入口后，确认：
    - `192.168.2.1:80` 是路由器 `K2P`；
    - `192.168.2.133` 是大老板家人的手机，不是 Home Assistant 主机；
    - `192.168.2.133` 当前只有 `80` 端口开，`5600` 与 `8123` 都未开；
  - 浏览器书签 / 历史中未找到可直接复用的 Home Assistant / `my.home-assistant` / `duckdns` / `nabucasa` 明确入口，只发现过旧访问痕迹 `http://192.168.2.133:5600/`，但当前已不可达。
- 今晚最关键的新事实已由大老板亲口确认：
  - 家里当前 **没有 Home Assistant**；
  - 自己手机上装的是 **天猫精灵 App**，但这不等于已具备 Home Assistant 控制端。
- 因此路线已正式调整，不再继续死磕不存在的 Home Assistant 环境：
  - **停止把 Home Assistant 当成当前环境里的必经主线**；
  - `Home Assistant -> 真机播放` 现在只保留为**未来可选接入方案**，不是明天继续投入时间的默认路线；
  - 当前项目口径正式收为：**桥接层已成，真机控制端未接入**。
- 今晚我已替大老板做出的主决策：
  1. 当前 `tmall-genie-voice-bridge` 先按**诚实可演示 MVP** 收口；
  2. 不再硬冲“立刻打通真天猫精灵硬件播放”；
  3. 下一阶段若继续攻坚，应改成独立课题：**寻找不依赖 Home Assistant 的真机控制入口**；
  4. 对外口径必须继续严格区分：
     - 已完成：bridge / callback / local playback / rehearsal / preflight / failure matrix；
     - 未完成：真天猫精灵硬件实际出声、人耳确认、最终真机 acceptance record。
- 明天继续时的固定起手顺序：
  1. 先按“可演示 MVP”口径整理当前项目状态总表；
  2. 再画出“无 Home Assistant 前提下”的真机接入方案树；
  3. 优先评估可行的官方 / 半官方 / 第三方控制入口；
  4. 若短期没有稳定控制端，就明确把真机接入列为下一阶段，不再虚报“只差最后一步”。
