

## 今日最终收尾补记（2026-04-05 00:22，准备关机前）

- 大老板今晚最后确认：**要睡觉了，准备关机**，要求把最后阶段工作再记清楚。

### 一、`desktop-input-control` 今晚最终状态（补充）

- 今晚对 `desktop-input-control` 的最关键真实结论已经进一步钉死：
  - **中文输入问题不是 QQ 特殊问题，而是 `desktop-input.py` 的中文输入本体本身未打通。**
- 已做过的最小真测包括：
  1. 在 Notepad 中直接执行 `type-text 新干线`
  2. 增加 `type-text-file`，用 UTF-8 文件绕过 argv 中文参数
  3. 单字测试（如：`新`）
  4. 常见短句测试（如：`你好`）
- 关键结论：
  - `argv` 传中文确实会坏；
  - 但即使绕过 argv，`SendInput` 返回成功、`lastError=0`，最终中文结果仍不可信；
  - 因此当前 `KEYEVENTF_UNICODE + SendInput` 这条中文输入实现**不能当作可用方案**。
- 今晚还明确形成长期判断：
  - **对“记事本里最终到底出现了什么字”的验证，控件层直接读文本比截图+OCR更可靠。**
- 剪贴板路线已按大老板要求删除，不再作为当前主线；以后不要误记成“剪贴板是当前默认输入方案”。

### 二、`tmall-genie-voice-bridge` 今晚最终状态（补充）

#### 1. edge-tts 已彻底做实
- 今晚多次真实调用 `scripts/speak.py` 成功。
- 本机播放返回：
  - `PLAYBACK_CONFIRMED=1`
  - `PLAYBACK_BACKEND=SoundPlayer`
  - `PLAYSTATE_FINAL=WAV_SYNC_OK`
- 大老板还明确人耳确认：
  - “刚才听到一个女的说话了”
  - “我听到了”
- 因此这条结论已经足够硬：
  - **edge-tts 真实接入成立**
  - **本机真实播报成立**
  - **人耳验收成立**

#### 2. 半闭环演示已成立
- 今晚已连续播报两轮固定回复：
  - `在`
  - `下雨了`
- 两轮都真实成功。
- 这意味着：
  - **回答端/播报端已经具备连续输出能力。**
- 但必须记住：
  - 这只是**半闭环演示**，不是完整真闭环。

#### 3. local_whisper 今晚的最准状态
- 已经确认：
  - `listen_once.py --engine local_whisper` 会真实执行；
  - `base` / `small` 模型都能跑起来；
  - 不是“Whisper 根本没接上”。
- 但质量验收没有通过。

##### 实验 A：拿 edge-tts 生成的 wav 反喂给 local_whisper
- 用过中英混杂样本，也用过纯中文样本：
  - `你好，我是沈万三。`
- 结果：
  - `base` / `small` 都会返回文本
  - 但文本整体表现为异常乱码 / 替换符风格，不可信
- 这说明：
  - **Whisper 真实执行成立**
  - **Whisper 转写质量/结果显示链未通过验收**

##### 实验 B：真实麦克风录音 -> local_whisper
- 已先检查麦克风：
  - 默认输入可访问：true
  - 检测到 `麦克风 (Logi C270 HD WebCam)`
- 第一轮录音：
  - `Local Whisper returned empty text`
- 第二轮录音：
  - 返回了部分中文：`要不就`
  - 说明输入链不是完全死的
- 但在回放该录音样本后，大老板确认：
  - **只有一点点噪音，没有人声**
- 这是今晚最关键的最终判断：
  - **真闭环当前失败的主因不是 Whisper 先听错，而是录音链路没有有效录到人声。**

#### 4. 关于 `araa47/local-whisper` skill 的查验结果
- 大老板后来提出 `https://clawhub.ai/araa47/local-whisper`，询问能否替代当前 `local_whisper`。
- 今晚已查清：
  - 这个 skill **本地已经安装**，路径正是：
    - `C:\Users\besam\.openclaw\workspace\skills\local-whisper`
  - 它不是一个全新的替代方案，而是当前已有 skill 本体。
- 今晚还直接尝试运行它自己的：
  - `scripts/transcribe.py`
  - 对刚才的麦克风录音样本做对照转写
- 结果报错：
  - `[WinError 2] 系统找不到指定的文件`
- 这意味着：
  - 这个 skill 不是“没装”，而是**已装但其直连脚本当前运行环境还有问题**。
- 但更重要的是：
  - 即便修好它，也**不能替代当前“录音样本里只有噪音、没有人声”的主问题**。

### 三、今晚对 `tmall-genie-voice-bridge` 的最终真实口径（睡前定稿）

#### 已成立
- edge-tts 真实接入：✅
- 本机真实播报：✅
- 人耳验收：✅
- 半闭环演示（固定回复：`在` / `下雨了`）：✅
- local_whisper 真实执行：✅

#### 未成立
- 真实听懂：`沈万三在吗`：❌
- “你说 -> 我听懂 -> 我答在” 第一轮真闭环：❌
- 完整两轮真闭环：❌

#### 当前最大主因
- **录音链路没有有效录到人声，wav 回放只有一点噪音。**
- 因此下一次继续这条线时，优先级必须改成：

1. **先修麦克风录音链路**
   - 先确认正确输入设备
   - 先录到清晰人声 wav
   - 先回放确认 wav 里真有人声
2. **再验 Whisper**
   - 只有在 wav 里确认有人声后，再用 local_whisper 做验收
3. **最后再测真闭环**
   - 先只攻第一句：`沈万三在吗` -> `在`
   - 第二句天气问答以后再说

### 四、附带问题（非主线，但值得记住）

- `tmall-genie-voice-bridge/scripts/play-local-audio.ps1` 今晚被发现存在 PowerShell 语法错误：
  - `UnexpectedToken }`
- 临时已改用 Python `winsound` 回放 wav。
- 这不是今晚主线，但后续应修。

### 五、今夜总收尾结论

- 今晚两条项目线里，最硬的真实成果是：
  1. **`desktop-input-control` 已确认当前中文输入本体不通，不能再误以为只差 QQ 坐标。**
  2. **`tmall-genie-voice-bridge` 已把输出链（edge-tts）做实，但真闭环失败主因已定位到录音链路。**
- 大老板现在要睡觉并准备关机，因此今晚所有工作以此为最终口径收尾，后续不要夸大完成度。
