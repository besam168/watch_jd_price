## 2026-04-17
- 大老板在 2026-04-16 明确提出：`a-share-live-smallcap` 的固定触发时间要改成 **每天 09:24:40**。
- 该项修改在本轮对话结束前**尚未完成落地验收**；后续继续时要优先定位该 skill 的真实定时入口（脚本内等待时间 + Windows 计划任务），避免只改一处导致实际仍按旧时间触发。
- 2026-04-14 已经把 `a-share-opening-flow-v6-auto` 邮件能力增强为更正式的盘前/早盘简报风格，并追加了：
  - 邮件标题规范化
  - “本阶段最值得盯的前 3 只”
  - 邮件末尾自动追加“操作建议：主看 / 跟随 / 不追”
  - 邮件末尾自动追加“风险提示：高开过猛 / 成交额虚高 / ST剔除提醒”等

## 2026-04-17
- 大老板在 2026-04-17 早晨提出：希望我调查 `Hermes` 的**控制入口**，目标是后续由我来操控 Hermes，让它成为我的执行层助手。
- 当前已形成的工作判断：Hermes 更像是**本地 agent / Python 环境式组件**，控制入口优先怀疑为以下几类：
  1. CLI 可执行入口（如 `hermes` / `hermes-agent` 或 venv 内脚本）
  2. Python 模块/脚本入口（如 `python xxx.py` / `python -m ...`）
  3. 本地服务入口（localhost HTTP API / WebSocket / 监听端口）
  4. 配置文件 + 启动脚本入口（`.bat` / `.ps1` / `.cmd` / `.json` / `.yaml` / `.env`）
- 今天已经实际核实到的 Hermes 线索如下：
  - 安装/参考文件：`C:\Users\besam\Desktop\hermes setup.txt`、`C:\Users\besam\Documents\anzo\hermes-install.sh`、`C:\Users\besam\Documents\anzo\hermes-wsl-install.sh`
  - 本机缓存痕迹：`C:\Users\besam\AppData\Local\uv\cache\...\hermes_agent-0.8.0...`
  - 安装脚本指向的项目为：`NousResearch/hermes-agent`
- 今天已经实际验证失败/未打通的点：
  - `where hermes`：无结果
  - `where hermes-agent`：无结果
  - `python -m pip show hermes-agent`：`Package(s) not found`
  - `C:\Users\besam\AppData\Local\Programs\Python\Python312\python.exe -m pip show hermes-agent`：`Package(s) not found`
  - `python -m pip show hermes_agent`：`Package(s) not found`
- 当前阶段性结论已收敛为：**这台 Windows 机器上存在 Hermes Agent 的安装脚本与 uv 缓存痕迹，但还没有形成可直接调用的正式安装入口；不能宣称已接管或已能稳定控制。**
- 后续继续时的默认动作顺序固定为：
  1. 先找 Hermes 在 Windows 上的正式安装方式（优先 `install.ps1` / 官方文档 / 仓库说明）
  2. 把 Hermes Agent 正式安装到可调用状态
  3. 验证真实入口（`hermes` / `hermes-agent` / `python -m hermes_agent` / venv `Scripts`）
  4. 再做最小真测，把它接成“我总控、Hermes 执行”的助手链路
- 本轮对话结束前，Hermes 的**最终可执行控制命令还未锁死验收**；后续继续时，要优先给出一份可落地清单：
  - Hermes 的真实目录位置
  - 可执行文件/启动脚本路径
  - 如果有 API，则记录端口与调用方式
  - 是否能接成“我负责总控，Hermes 做执行”的稳定协作模式
- 大老板已明确：**今天先把这项工作记录好，明天再继续搞。**

## 2026-04-17
- 大老板进一步明确要求：把这次 `zhangxuefeng-skill` 的制作经验沉淀下来，后续还会继续做**类似“名人风格 + 方法论”型技能**，要保证可以复用。
- 这次对 `C:\Users\besam\.openclaw\workspace\skills\zhangxuefeng-skill` 的复盘，已经确认一条可长期复用的方法论：**这类技能优先做成“人格化咨询 skill + 研究资料包”，而不是一上来写重型程序。**
- 可复用的标准制作流程固定为：
  1. **先定技能目标**：不是泛泛模仿名人语气，而是明确这个技能到底要复现对方的哪种能力（如咨询、点评、劝退、分析、写作、演讲、谈判等）
  2. **再拆人格与方法论**：至少拆成 5 层——表达层、价值层、判断层、流程层、边界层
  3. **研究资料分层沉淀**：把资料沉淀到 `references/research/`，至少分出“表达 DNA”“决策模型/方法论”“案例/争议/边界”“来源索引”几类
  4. **用 `SKILL.md` 固化运行规则**：写清楚触发条件、任务目标、输出口径、禁区、流程顺序，而不是只写一句模糊 prompt
  5. **先做 prompt/知识驱动版本，再决定是否加脚本**：如果只是风格化咨询或内容生成，优先走 skill 文档 + 研究资料；只有当用户需要结构化数据、外部检索、自动化执行时，再补脚本/API
  6. **验收时分开检查“像不像”和“准不准”**：既要看说话风格是否像本人，也要看判断逻辑是否真的像本人，不能只学口头禅
- 当前已确认：`zhangxuefeng-skill` 的核心原理不是复杂代码，而是**结构化 prompt + 研究资料约束 + 角色化输出**；真正决定质量的不是“学几句名人口头禅”，而是是否抓住了对方的**价值取向、判断逻辑和输出节奏**。
- 以后再做类似 skill，默认优先建立如下目录骨架：
  - `SKILL.md`
  - `references/research/01-writings.md`
  - `references/research/02-style.md` 或同类表达研究
  - `references/research/03-expression-dna.md`
  - `references/research/04-cases.md`
  - `references/research/05-decisions.md`
  - `references/research/06-boundaries.md`
  - `references/research/99-sources.md`
- 以后做这类技能时，默认要先回答 6 个问题：
  1. 这个名人最值得复现的能力是什么？
  2. 他的建议/输出背后的核心价值排序是什么？
  3. 他做判断时最常用的取舍标准是什么？
  4. 他的表达风格里，哪些是表皮，哪些是骨架？
  5. 哪些话术/立场是高风险区，不能机械复刻？
  6. 这个技能最终是给谁用、用在什么场景？
- 长期原则固定为：**先抽象方法论，再复制人格；先做稳，再做像。** 只模仿名人口癖，skill 会很空；抓住“判断框架 + 表达 DNA + 使用场景”，skill 才能复用、能升级。
- 2026-04-17 09:23 左右，已按上述方法在工作区落地一个可复用模板目录：`C:\Users\besam\.openclaw\workspace\skills\celebrity-method-skill-template`。
- 该模板已包含：
  - `SKILL.md`（通用运行规则模板）
  - `README.md`（使用说明）
  - `references/research/01-writings.md`
  - `references/research/02-conversations.md`
  - `references/research/03-expression-dna.md`
  - `references/research/04-external-views.md`
  - `references/research/05-decisions.md`
  - `references/research/06-boundaries.md`
  - `references/research/07-scenarios.md`
  - `references/research/99-sources.md`
- 以后大老板要再做类似“名人风格 + 方法论”技能时，默认优先从这个模板目录复制，再替换 `<名人名字>`、`<核心能力>`、`<价值排序>`、`<高频判断标准>`、`<风险边界>` 等占位项。
