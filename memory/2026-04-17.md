- 为了让 `heartbeat` 驱动的 `self-evolution` 有稳定落点，已在 `C:\Users\besam\.openclaw\workspace\self-evolution\learning-log.md` 中新增 `## Heartbeat 推进记录` 区域，并提供固定模板。
- 以后每次 heartbeat 若对自我进化有实质推进，应优先把结果记到该区域，形成“轻自动推进 -> 有记录可追 -> 能继续接力”的闭环。
- 2026-04-17 下午：`self-evolution` 已正式整理出可复用 skill 原型 `skills/self-evolution-radar`，当前包含 `SKILL.md`、`README.md`、`scripts/run_real_patrol.py` 与 `references/` 资料集。
- 同一轮已将 `run_real_patrol.py` 升级为参数化 V1，现支持 `--topic`、`--source`、`--reddit-limit`、`--github-limit`、`--output`，并已最小真测通过，生成结果文件：`C:\Users\besam\.openclaw\workspace\skills\self-evolution-radar\patrol-run-agent-2026-04-17.txt`。
- 随后又继续推进到 V1 / V2 过渡版：`run_real_patrol.py` 现默认可同时产出**原始巡逻文本 + Markdown 结构化摘要**，并新增 `--summary-output`、`--no-summary` 参数；已真测生成摘要文件：`C:\Users\besam\.openclaw\workspace\skills\self-evolution-radar\patrol-summary-agent-2026-04-17.md`。
- 2026-04-17 晚些时候，大老板又把 `self-evolution` 的主题方向进一步钉死：它的默认主线不是“单纯做外部巡逻”，而是**与实际工作结合的自我进化**——真实工作里发现卡点 → 当场解决 → 沉淀成脚本 / skill / 模板 / 工作流 → 下次更稳、更快、更强。
- 这条默认路线以后应写死执行；Reddit / GitHub 巡逻只是其中一个输入源，作用是帮真实工作补能力缺口、找实现、找可回灌方法，不能把 `self-evolution` 狭义理解成“刷外部资讯”。
- 同时要长期遵守 4 条硬原则：1）自我进化必须服务实际工作；2）每次进化都要落到工作流；3）小改进也要记账；4）遇到问题不能只绕过去，还要补能力缺口。
- 2026-04-17 本轮继续落地了“工作卡点入口”：新增 `self-evolution/worklog.md` 与 `self-evolution/bottleneck-template.md`，用于把真实工作里的卡点固定收口成可追踪记录，而不只停留在聊天里。
- `worklog.md` 当前已补入一条首个示例：围绕“搜索 / 抓取路线不稳”反推 `self-evolution` 主线，明确以后遇到同类问题要同步判断能否沉淀成脚本、模板、skill 或备用路径。
- `self-evolution/README.md` 也已同步新增“工作卡点入口”说明，当前主线已从抽象原则推进到可执行记录入口。
- 随后继续补了 `self-evolution/worklog-radar-loop.md`，把“真实工作卡点 -> watchlist/topic 判断 -> self-evolution-radar -> summary -> 回写 worklog”这条联动路径正式写成说明文件。
- `self-evolution/worklog.md`、`self-evolution/README.md`、`skills/self-evolution-radar/README.md` 已同步接上这条联动口径；现在 radar 被明确放回正确位置：它负责外部输入，不是脱离实际工作的独立逛街动作。
- 2026-04-17 本轮又正式完成了第一条完整真闭环样板：以“搜索 / 抓取路线不稳”为真实工作卡点，完成了 `worklog -> radar -> summary -> 回写 worklog` 全链路，说明这套 `self-evolution` 已从原则层推进到**至少可跑一条真实样板闭环**。
- 这条首个样板当前沉淀出的明确下一资产是：继续做“搜索 / 抓取不稳时的备用工作流”，而不是停留在抽象反思。
- 这一资产现已正式落盘为：`self-evolution/fallback-search-and-crawl-workflow.md`，里面已定义初版执行顺序：先判断任务类型 -> 主路线只快速试 1 次 -> 切轻量替代路线 -> 再切本机可控路线 -> 若已形成能力缺口则进入 `worklog -> radar -> 回写` 闭环。
- `self-evolution/README.md`、`self-evolution/worklog.md`、`skills/self-evolution-radar/README.md` 已同步接入这份 fallback 文档；现在这条真实样板已经不只是“有结论”，而是开始长出明确 SOP。
- 2026-04-17 大老板进一步明确了更高一层目标：`self-evolution` 不应只服务当前这个闭环 skill，还应作为一个方法原型，未来尽可能推广到整个 OpenClaw 系统。当前应坚持“先在单个闭环里做实，再逐步推广到多 skill / 全系统”的节奏，不能直接吹成系统级成熟能力。
- 基于这个目标，已新增系统蓝图文档：`self-evolution/openclaw-self-evolution-architecture.md`，正式把“单闭环验证层 -> 多 skill 复用层 -> OpenClaw 系统方法层”三层演进路线写清楚，并区分了方法层与具体 skill 执行层。
- 大老板随后又进一步明确：`self-evolution` 还必须拥有一块**自我进化长期记忆层**（Self-Evolution Long-Term Memory Layer, **SE-LTM Layer**），否则只能算会记录问题，不能算真正会积累能力。基于此，已新增：`self-evolution/se-ltm-layer.md` 与 `self-evolution/se-ltm-layer/` 目录骨架，用于沉淀 bottleneck history、fallback history、improvement assets、reusable patterns、promotion candidates。
- 当前阶段定性：`self-evolution-radar` 属于“可复用的外部巡逻 skill 原型 V1/V1.5”，不是成熟产品；已能做参数化素材巡逻，但尚未完成结构化报告、自动摘要、可配置 watchlist 与定时调度。
- 大老板已明确：这个项目今晚再继续推进；下一步优先候选为：1）结构化报告输出；2）watchlist / preset 配置外置化。
- 2026-04-17 傍晚又补了一个很小但真实有用的能力资产：当用户追问“当前会话模型 / provider / 连接地址”时，单看 `models.json` 容易被旧配置或静态文件误导；因此已新增 `self-evolution/openclaw-model-route-debug.md`，把排查顺序固定为：先看 `openclaw status` / 会话状态，再看 `openclaw config get models` 的合并结果，再看 `agents.defaults.model`，最后才看本地文件。以后遇到同类问题，优先走这份 SOP。
- 2026-04-17 晚间 heartbeat 又补了一步小而实的推进：对 `self-evolution-radar` 做了第三轮功能测试，正式验证 `--no-summary` 模式可用。实测命令为：`python .\skills\self-evolution-radar\scripts\run_real_patrol.py --topic memory --source github --github-limit 4 --output .\skills\self-evolution-radar\test-memory-raw.txt --no-summary`；结果确认 raw 文件会生成、summary 文件不会生成。
- 这意味着当前 `self-evolution-radar` 脚本层已经实测通过的能力包括：最小运行、topic 切换、source 切换、自定义输出路径、可选关闭摘要。下一步应从“参数面测试”转向“真实闭环联动测试”，不要继续只堆接口验收。
- 随后已正式完成第四轮真实闭环联动测试：从 `worklog` 中“搜索 / 抓取路线不稳”这条真实卡点出发，重新执行 `agent` topic 巡逻，产出：`skills/self-evolution-radar/loop-test-agent-raw.txt` 与 `skills/self-evolution-radar/loop-test-agent-summary.md`，再将结论回写到 `worklog.md`。
- 这轮验证确认的关键不是“脚本能运行”，而是：`worklog -> radar -> summary -> 回写` 这条链路在真实卡点上可复现，说明 `self-evolution-radar` 已不仅是独立脚本原型，而是能真正接进 `self-evolution` 的闭环中。
- 2026-04-17 深夜又补出一份很实的产品化 SOP：`self-evolution/skill-publish-workflow.md`。起因是第三方 skill 在工作区内改造后再发 GitHub 时，连续暴露出两个稳定坑：一是目录自带 `.git` 容易被主工作区误识别成 embedded repo / submodule，二是总工作区很脏，直接从根目录做发布会把边界搞乱。
- 这份 SOP 现已把顺序固定为：**先收口 skill 内容 -> 检查/移除旧 `.git` -> 不在总工作区直接推 -> 以 skill 目录为边界独立 init / commit / push**。以后只要做“第三方 skill 改造后再独立发布”，默认优先按这份文档走，而不是临场靠记忆操作。
- 同一晚又补出一份 Claude Code 路由配置 SOP：`self-evolution/claude-code-route-config-workflow.md`。起因是刚刚把 Claude Code 切到第三方兼容供应方时，再次验证了一个稳定规律：**用户给的 `/v1` 地址，不一定就是 Claude Code 最稳的填写方式；Claude Code 往往更适合填根地址，再由它自己拼后续路径。**
- 这份 workflow 现已把顺序固定为：**先查 `.claude/settings.json` -> 默认优先根地址 -> 统一模型族字段 -> 立刻执行 `claude -p` 最小真测**。以后凡是给 Claude Code 切模型 / 供应方，默认优先按这份文档走，没真测成功就不算真的配好。
- 同一晚又补出一份 Codex CLI 验收口径 SOP：`self-evolution/codex-cli-exec-completion-workflow.md`。起因是 `codex exec` 最小真测里已经拿到 `CODEX_ROUTE_OK`，说明请求层成功，但 tool 进程没有及时自然退出，随后还出现了 `SIGKILL` 系统消息。
- 这份 workflow 现已把口径固定为：**先判断模型有没有回到目标文本，再判断 CLI 进程有没有干净退出；请求层成功不等于进程层成功。** 以后做 Codex CLI 真测，默认必须按这两层写验收结论，避免把“模型已返回”误说成“整条 exec 生命周期完全正常”。
